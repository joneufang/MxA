import { Delta } from "./deltas";
import { AbstractModel } from "../abstract-model";
export declare class DeltaReverser {
    model: AbstractModel;
    constructor(model: AbstractModel);
    reverse(delta: Delta): Delta;
    private reverseUpdatePropertyValueDelta(delta, unit);
    private reverseCreateElementTreeDelta(delta);
    private reverseDeleteElementDelta(delta, unit);
    private reverseAttachElementDelta(delta);
    private reverseDetachElementDelta(delta, unit);
}
