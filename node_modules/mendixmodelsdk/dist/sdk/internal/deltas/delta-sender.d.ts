import { Delta } from "./deltas";
import { common } from "../../../common";
import { AbstractModel } from "../abstract-model";
export declare class DeltaSender {
    private model;
    private closeCallback;
    private errorCallback;
    isClosing: boolean;
    private hasError;
    private queue;
    pending: boolean;
    constructor(model: AbstractModel, closeCallback: common.IVoidCallback, errorCallback: common.IErrorCallback);
    closeConnection(): void;
    scheduleDeltas(deltas: Delta[]): void;
    private processQueue();
    private sendDeltas(deltas);
}
