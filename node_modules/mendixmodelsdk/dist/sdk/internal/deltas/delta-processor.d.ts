import { AbstractModel } from "../abstract-model";
import { Delta } from "./deltas";
export declare class DeltaProcessor {
    model: AbstractModel;
    constructor(model: AbstractModel);
    processDeltas(deltas: Delta[]): void;
    private processUpdatePropertyValueDelta(unit, delta);
    private processMutator(structure, property, mutator);
    private processCreateElementTreeDelta(unit, delta);
    private processDeleteElementDelta(unit, delta);
    private processAttachElementDelta(unit, delta);
    private processDetachElementDelta(unit, delta);
}
