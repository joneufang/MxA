import { AbstractModel } from "./abstract-model";
import { Delta } from "../internal";
export declare class TransactionChecks {
    private model;
    checkDetachedStructures: boolean;
    private detachedRequiredProperties;
    constructor(model: AbstractModel);
    check(queue: Delta[]): void;
    onNewDelta(delta: Delta): void;
}
